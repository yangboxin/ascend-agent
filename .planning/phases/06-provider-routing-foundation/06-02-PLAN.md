---
phase: 06-provider-routing-foundation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/ascend_agent/cli/diagnose.py
  - src/ascend_agent/cli/fix.py
  - src/ascend_agent/cli/reproduce.py
  - src/ascend_agent/cli/verify.py
  - tests/test_cli.py
autonomous: true
requirements: [PROV-03, PROV-04]
user_setup: []

must_haves:
  truths:
    - "All 4 CLI commands (diagnose, fix, reproduce, verify) use create_router() instead of direct ModelRouter() construction"
    - "Each CLI command reads provider from ctx.obj with per-command --provider override"
    - "Missing API key errors still show actionable Rich error messages"
    - "CLI integration tests verify --provider flag at root and per-command level"
  artifacts:
    - path: "src/ascend_agent/cli/diagnose.py"
      provides: "create_router wiring with ctx.obj provider resolution"
      contains: "create_router(provider="
    - path: "src/ascend_agent/cli/fix.py"
      provides: "create_router wiring with ctx.obj provider resolution"
      contains: "create_router(provider="
    - path: "src/ascend_agent/cli/reproduce.py"
      provides: "create_router wiring with ctx.obj provider resolution"
      contains: "create_router(provider="
    - path: "src/ascend_agent/cli/verify.py"
      provides: "create_router wiring with ctx.obj provider resolution"
      contains: "create_router(provider="
    - path: "tests/test_cli.py"
      provides: "--provider flag integration tests"
      min_lines: 310
  key_links:
    - from: "app.py ctx.obj['provider']"
      to: "diagnose_run ctx.obj.get('provider')"
      via: "typer.Context injection"
      pattern: "ctx\\.obj\\.get\\(\"provider\""
    - from: "diagnose.py create_router()"
      to: "Engine(router=...)"
      via: "router = create_router(provider=...)"
      pattern: "create_router\\(provider"
    - from: "fix.py create_router()"
      to: "FixEngine(router=...)"
      via: "router = create_router(provider=...)"
      pattern: "create_router\\(provider"

---

<objective>
Wire all 4 CLI commands (diagnose, fix, reproduce, verify) to use `create_router()` with context-aware provider resolution, add per-command `--provider` overrides, and add CLI integration tests for the `--provider` flag.

**Purpose:** Complete the two-level `--provider` flag system (D-03, D-04) and replace all direct `ModelRouter()` constructions with the new `create_router()` factory, enabling multi-provider routing from the CLI.

**Output:** Updated `diagnose.py`, `fix.py`, `reproduce.py`, `verify.py` — all using `create_router()` with ctx-based provider resolution and per-command overrides. New CLI tests for `--provider` flag behavior.
</objective>

<execution_context>
@/Users/ybx/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ybx/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-provider-routing-foundation/06-CONTEXT.md (D-03, D-04)
@.planning/phases/06-provider-routing-foundation/06-PATTERNS.md (diagnose/fix/reproduce/verify analogs)
@src/ascend_agent/cli/diagnose.py
@src/ascend_agent/cli/fix.py
@src/ascend_agent/cli/reproduce.py
@src/ascend_agent/cli/verify.py
@src/ascend_agent/cli/app.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire diagnose.py and fix.py to use create_router() with per-command --provider override</name>

  <files>
    src/ascend_agent/cli/diagnose.py
    src/ascend_agent/cli/fix.py
  </files>

  <read_first>
    @src/ascend_agent/cli/diagnose.py
    @src/ascend_agent/cli/fix.py
    @.planning/phases/06-provider-routing-foundation/06-PATTERNS.md (diagnose analog: lines 269-287, fix analog: lines 305-314)
    @.planning/phases/06-provider-routing-foundation/06-CONTEXT.md (D-03, D-04)
  </read_first>

  <action>
    Modify `src/ascend_agent/cli/diagnose.py`:

    1. Change `from ascend_agent.diagnosis.router import ModelRouter` to `from ascend_agent.diagnosis.router import create_router`.
    2. Add `from typing import Optional` to imports (for the per-command --provider flag type hint).
    3. Modify `diagnose_run()` function signature:
       - Add `ctx: typer.Context` as the first parameter (after any existing positional params — Typer injects context)
       - Add `provider: Optional[str] = typer.Option(None, "--provider", help="LLM provider (overrides root --provider)")` param
    4. Add provider resolution before router construction:
       ```python
       resolved_provider = provider or (ctx.obj.get("provider", "openai") if ctx.obj else "openai")
       ```
    5. Replace `router = ModelRouter()` with `router = create_router(provider=resolved_provider)`
    6. Update the ValueError hint message to be provider-agnostic: change `"[yellow]Hint: Set the OPENAI_API_KEY environment variable to use the diagnosis engine.[/yellow]"` to `"[yellow]Hint: Set the appropriate API key environment variable for provider '{resolved_provider}'.[/yellow]"`
    7. The existing try/except ValueError block stays structurally the same. The optional `output` path logic stays unchanged.
    8. Note: `_one_shot_mode()` is called by `diagnose_run()` — since provider resolution happens in `diagnose_run()`, it must pass `resolved_provider` to `_one_shot_mode()` or the router construction must move into `diagnose_run()`. Actually, looking at the code flow: `diagnose_run()` calls either `_repl_mode(repo)` or `_one_shot_mode(repo, trace, trace_text, output)`. The router construction is inside `_one_shot_mode()` (line 76). So either:
       - Pass `resolved_provider` as a parameter to `_one_shot_mode()` and use it there, OR
       - Move the router construction + engine call into `diagnose_run()` before dispatching.

       **Preferred approach:** Pass `resolved_provider` as a parameter to `_one_shot_mode`. Update `_one_shot_mode` signature to accept `provider: str`, and use `create_router(provider=provider)` inside.

       So:
       - Add `provider: str = "openai"` parameter to `_one_shot_mode()`
       - Pass `resolved_provider` from `diagnose_run()` to `_one_shot_mode()`
       - Update the route construction in `_one_shot_mode` to use `create_router(provider=provider)`

    Modify `src/ascend_agent/cli/fix.py`:

    1. Change `from ascend_agent.diagnosis.router import ModelRouter` to `from ascend_agent.diagnosis.router import create_router`.
    2. Add `from typing import Optional` to imports.
    3. Modify `fix_run()` function signature:
       - Add `ctx: typer.Context` as the first parameter
       - Add `provider: Optional[str] = typer.Option(None, "--provider", help="LLM provider (overrides root --provider)")` param
    4. Add provider resolution before router construction (after the diagnosis_input parsing block, before step 3):
       ```python
       resolved_provider = provider or (ctx.obj.get("provider", "openai") if ctx.obj else "openai")
       ```
    5. Replace `router = ModelRouter(model=os.environ.get("ASCEND_FIX_MODEL", "gpt-4o"))` with `router = create_router(provider=resolved_provider)`
       - Per D-04, `--provider` overrides per-engine model env vars. The provider's `default_model` from its `ProviderConfig` is used instead of `ASCEND_FIX_MODEL`.
    6. Update the ValueError hint message: change to `f"[yellow]Hint: Set the appropriate API key environment variable for provider '{resolved_provider}'.[/yellow]"`
    7. All other logic (diagnosis JSON parsing, review workflow, batch apply, output saving) stays unchanged.

    No changes needed to `_run_review_workflow`, `_apply_fixes_batch`, or `_save_fixes_output`.

    Verify with the test command before declaring task done.
  </action>

  <verify>
    <automated>python -m pytest tests/test_cli.py -v -k "diagnose or fix" --no-header -q 2>&1 | tail -15</automated>
  </verify>

  <done>
    - diagnose.py imports `create_router` instead of `ModelRouter`
    - diagnose_run() accepts `ctx: typer.Context` and `--provider` option
    - _one_shot_mode() accepts `provider` parameter and uses `create_router(provider=...)`
    - fix.py imports `create_router` instead of `ModelRouter`
    - fix_run() accepts `ctx: typer.Context` and `--provider` option
    - fix_run() uses `create_router(provider=resolved_provider)` instead of `ModelRouter(model=...)`
    - Error messages reference the resolved provider name instead of hardcoded OPENAI_API_KEY
  </done>

  <acceptance_criteria>
    - `grep -c "create_router" src/ascend_agent/cli/diagnose.py` returns 1
    - `grep -c "create_router" src/ascend_agent/cli/fix.py` returns 1
    - `grep -c "ctx.obj" src/ascend_agent/cli/diagnose.py` returns 1
    - `grep -c "ctx.obj" src/ascend_agent/cli/fix.py` returns 1
    - `grep -c "resolved_provider" src/ascend_agent/cli/diagnose.py` returns 1
    - `grep -c "resolved_provider" src/ascend_agent/cli/fix.py` returns 1
    - `grep -c "ModelRouter" src/ascend_agent/cli/diagnose.py` returns 0
    - `grep -c "ModelRouter" src/ascend_agent/cli/fix.py` returns 0
    - All existing diagnose + fix CLI tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire reproduce.py and verify.py to use create_router() and add --provider CLI integration tests</name>

  <files>
    src/ascend_agent/cli/reproduce.py
    src/ascend_agent/cli/verify.py
    tests/test_cli.py
  </files>

  <read_first>
    @src/ascend_agent/cli/reproduce.py
    @src/ascend_agent/cli/verify.py
    @tests/test_cli.py
    @.planning/phases/06-provider-routing-foundation/06-PATTERNS.md (reproduce analog: lines 328-338, verify analog: lines 350-360, CLI tests analog: lines 422-467)
    @.planning/phases/06-provider-routing-foundation/06-CONTEXT.md (D-03, D-04)
  </read_first>

  <action>
    Modify `src/ascend_agent/cli/reproduce.py`:

    1. Change `from ascend_agent.diagnosis.router import ModelRouter` to `from ascend_agent.diagnosis.router import create_router`.
    2. Add `from typing import Optional` to imports.
    3. Modify `reproduce_run()` function signature:
       - Add `ctx: typer.Context` as the first parameter
       - Add `provider: Optional[str] = typer.Option(None, "--provider", help="LLM provider (overrides root --provider)")` param
    4. Add provider resolution before router construction (after `repo_path` extraction, before the existing try/except ValueError block):
       ```python
       resolved_provider = provider or (ctx.obj.get("provider", "openai") if ctx.obj else "openai")
       ```
    5. Replace `router = ModelRouter()` with `router = create_router(provider=resolved_provider)`
    6. Update the hint message from `"[yellow]Hint: Set the OPENAI_API_KEY environment variable.[/yellow]"` to `f"[yellow]Hint: Set the appropriate API key environment variable for provider '{resolved_provider}'.[/yellow]"`
    7. All other logic (JSON parsing, engine execution, output display, output saving) stays unchanged.

    Modify `src/ascend_agent/cli/verify.py`:

    1. Change `from ascend_agent.diagnosis.router import ModelRouter` to `from ascend_agent.diagnosis.router import create_router`.
    2. Add `from typing import Optional` to imports.
    3. Modify `verify_run()` function signature:
       - Add `ctx: typer.Context` as the first parameter
       - Add `provider: Optional[str] = typer.Option(None, "--provider", help="LLM provider (overrides root --provider)")` param
    4. Add provider resolution before router construction (after `repo_path` extraction, before the existing try/except ValueError block):
       ```python
       resolved_provider = provider or (ctx.obj.get("provider", "openai") if ctx.obj else "openai")
       ```
    5. Replace `router = ModelRouter()` with `router = create_router(provider=resolved_provider)`
    6. Update the hint message from `"[yellow]Hint: Set the OPENAI_API_KEY environment variable.[/yellow]"` to `f"[yellow]Hint: Set the appropriate API key environment variable for provider '{resolved_provider}'.[/yellow]"`
    7. All other logic (JSON parsing, engine execution, output display, exit code) stays unchanged.

    Modify `tests/test_cli.py`:

    Add these new test functions after existing tests (at end of file):

    1. `test_cli_diagnose_root_provider_flag(tmp_path, monkeypatch)`:
       - Creates a `tmp_path/test.py` file
       - Mocks `Engine` as lambda returning a Mock with `.diagnose.return_value` having hypotheses=[], errors=[], iterations_used=0
       - Monkeypatches `create_router` to return a Mock (instead of `ModelRouter.__init__`)
       - Uses `runner.invoke(app, ["--provider", "deepseek", "diagnose", "run", str(tmp_path), "--trace-text", "Error: test"])`
       - Asserts `result.exit_code == 0`
       - Follows existing pattern from `test_cli_diagnose_run_basic` but uses `create_router` mock instead of `ModelRouter.__init__`

    2. `test_cli_diagnose_per_command_provider_override(tmp_path, monkeypatch)`:
       - Same setup as above
       - Uses `runner.invoke(app, ["--provider", "openai", "diagnose", "run", "--provider", "deepseek", str(tmp_path), "--trace-text", "Error: test"])`
       - Asserts `result.exit_code == 0`
       - Verifies per-command --provider overrides root --provider

    3. `test_cli_provider_flag_help()`:
       - Uses `runner.invoke(app, ["--help"])`
       - Asserts `"--provider"` appears in stdout
       - Uses `runner.invoke(app, ["diagnose", "run", "--help"])`
       - Asserts `"--provider"` appears in stdout (per-command override listed)

    Use the existing test infrastructure: `runner = CliRunner()`, `from ascend_agent.cli.app import app`, `monkeypatch.setattr`.

    Follow the established pattern for mocking: mock `create_router` at the module level where it's imported (e.g., `monkeypatch.setattr("ascend_agent.cli.diagnose.create_router", lambda provider: Mock())`).

    Verify with the test command before declaring task done.
  </action>

  <verify>
    <automated>python -m pytest tests/test_cli.py -v --no-header -q 2>&1 | tail -20</automated>
  </verify>

  <done>
    - reproduce.py imports `create_router` instead of `ModelRouter`
    - verify.py imports `create_router` instead of `ModelRouter`
    - Both commands accept `ctx: typer.Context` and `--provider` option
    - Both use `create_router(provider=resolved_provider)`
    - Error messages reference provider name
    - 3 new CLI tests verify --provider flag at root, per-command, and help levels
    - All existing CLI tests still pass
  </done>

  <acceptance_criteria>
    - `grep -c "create_router" src/ascend_agent/cli/reproduce.py` returns 1
    - `grep -c "create_router" src/ascend_agent/cli/verify.py` returns 1
    - `grep -c "ModelRouter" src/ascend_agent/cli/reproduce.py` returns 0
    - `grep -c "ModelRouter" src/ascend_agent/cli/verify.py` returns 0
    - `grep -c "test_cli_diagnose_root_provider_flag" tests/test_cli.py` returns 1
    - `grep -c "test_cli_diagnose_per_command_provider_override" tests/test_cli.py` returns 1
    - `grep -c "test_cli_provider_flag_help" tests/test_cli.py` returns 1
    - `python -m pytest tests/test_cli.py -v --no-header -q 2>&1 | grep -c "passed"` shows all tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → engine wiring | User-supplied --provider flag determines which provider config is resolved |
| Stderr error messages | CLI displays provider-specific error hints on missing API keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | error messages | mitigate | Error hints reference provider name but never include API key values. Messages are templated strings with no user-controlled interpolation. |
| T-06-05 | Tampering | CLI --provider flag | mitigate | --provider only selects env var prefix (e.g., "deepseek" → ASCEND_DEEPSEEK_*). The flag value is passed to create_router() which reads env vars — no shell injection or file access from flag value. |
| T-06-SC | Tampering | no new dependencies | mitigate | Plan 06-02 adds zero new pip/npm packages. All changes are to existing project files with existing dependencies. No package gate needed. |
</threat_model>

<verification>
Run full test_cli.py: `python -m pytest tests/test_cli.py -v --no-header -q 2>&1`
Grep for ModelRouter in all 4 CLI files — should be zero: `grep -r "ModelRouter" src/ascend_agent/cli/`
Verify --provider appears in help: `python -c "from ascend_agent.cli.app import app; from typer.testing import CliRunner; r = CliRunner().invoke(app, ['diagnose', 'run', '--help']); assert '--provider' in r.stdout; print('OK')"`
</verification>

<success_criteria>
- All 4 CLI files import `create_router` and use `create_router(provider=resolved_provider)` — zero direct `ModelRouter()` instantiations in CLI layer
- Per-command `--provider` flag visible in `ascend-agent diagnose run --help`
- All CLI tests pass (existing + 3 new)
- Provider-aware error messages displayed when API key is missing
</success_criteria>
