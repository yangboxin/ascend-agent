---
phase: 04-reproduction-capability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/ascend_agent/config.py]
autonomous: false
requirements: [REPRO-01]
user_setup:
  - service: asyncssh
    why: "Remote SSH execution per D-05"
    env_vars: []
    dashboard_config:
      - task: "Verify asyncssh>=2.23.0 is legitimate"
        location: "https://pypi.org/project/asyncssh/"

must_haves:
  truths:
    - "pyproject.toml lists asyncssh>=2.23.0 as a runtime dependency"
    - "asyncssh is installed and importable in the project environment"
    - "Settings class has ssh_host, ssh_user, ssh_key_path fields with ASCEND_ prefix"
    - "Settings class has shell_timeout field defaulting to 60"
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependency declaration for asyncssh"
      contains: "asyncssh>=2.23.0"
    - path: "src/ascend_agent/config.py"
      provides: "Settings with SSH connection parameters"
      contains: "ssh_host: str"
      contains: "shell_timeout: int = Field(default=60"
  key_links:
    - from: "env ASCEND_SSH_HOST"
      to: "Settings.ssh_host"
      via: "SettingsConfigDict(env_prefix='ASCEND_')"
    - from: "env ASCEND_SHELL_TIMEOUT"
      to: "Settings.shell_timeout"
      via: "SettingsConfigDict(env_prefix='ASCEND_')"
---

<objective>
Add the asyncssh dependency and SSH configuration fields to the project foundation.

Purpose: Unblocks all remote execution work — exec_shell and ReproductionEngine need asyncssh installed and SSH config available on the Settings singleton.

Output: Modified pyproject.toml with asyncssh dependency, asyncssh installed via pip, and Settings class extended with ssh_host, ssh_user, ssh_key_path, shell_timeout fields per D-06.
</objective>

<execution_context>
@/Users/ybx/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ybx/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-reproduction-capability/04-CONTEXT.md
@.planning/phases/04-reproduction-capability/04-RESEARCH.md
@.planning/phases/04-reproduction-capability/04-PATTERNS.md

# Key references for tasks
@src/ascend_agent/config.py
@pyproject.toml

# Pattern references
@04-PATTERNS.md Section 5 (config.py SSH fields — lines 282-324)
@04-PATTERNS.md Section 7 (pyproject.toml dependency — lines 426-453)
@04-RESEARCH.md Package Legitimacy Audit (lines 88-98) — asyncssh tagged [ASSUMED]
@04-RESEARCH.md Environment Availability (lines 703-718) — Python 3.10+ required
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add asyncssh>=2.23.0 to pyproject.toml dependencies</name>
  <files>pyproject.toml</files>
  <read_first>
src/ascend_agent/config.py    — current Settings class to understand existing patterns

pyproject.toml    — current dependencies section, exact line positions

.planning/phases/04-reproduction-capability/04-PATTERNS.md   — Section 7 lines 426-453 for exact insertion pattern

.planning/phases/04-reproduction-capability/04-RESEARCH.md   — lines 86-98 for version verification, lines 703-718 for env awareness
  </read_first>
  <action>
Add the asyncssh dependency to pyproject.toml.

Insert the string "asyncssh>=2.23.0" as a new list item in the `dependencies` array after the existing "pydantic-settings>=2.14.0" entry and before the closing bracket.

The insertion point is between line 16 ("pydantic-settings>=2.14.0",) and line 17 (]). Add a trailing comma to the new entry to match the existing style.

Note: asyncssh 2.23.0 requires Python >=3.10 (research A1). The project already declares `requires-python = ">=3.10"` (line 9). If the current environment uses Python 3.9.6 (system default), a Python 3.10+ runtime (e.g., venv created with a 3.10+ interpreter) is needed. This is surfaced here but handled at Task 2 install time.
  </action>
  <verify>
    <automated>grep -c 'asyncssh>=2.23.0' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
- `pyproject.toml` line `dependencies = [` block contains `"asyncssh>=2.23.0",`
- The insertion preserves alphabetical grouping (asyncssh placed after pydantic-settings, before closing bracket)
- Running `grep 'asyncssh' pyproject.toml` returns exactly 1 match
  </acceptance_criteria>
  <done>The pyproject.toml dependencies list includes asyncssh>=2.23.0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install asyncssh (package legitimacy gate)</name>
  <what-built>
The executor will run `pip install "asyncssh>=2.23.0"` to install the asyncssh package. This checkpoint gates the install because asyncssh was flagged [ASSUMED] in the Package Legitimacy Audit — the slopcheck tool was unavailable at research time, so the package could not be automatically verified.
  </what-built>
  <how-to-verify>
1. The executor has already attempted `pip install "asyncssh>=2.23.0"`
2. Verify the package on PyPI: visit https://pypi.org/project/asyncssh/
   - Confirm the package name is "asyncssh" (not a typo-squatted variant)
   - Confirm version 2.23.0 exists (released 2026-05-09)
   - Confirm the source repository is github.com/ronf/asyncssh
   - Confirm ~1.7k GitHub stars, established since 2015
3. If using Python 3.9.6 (system default), the install will fail because asyncssh requires >=3.10. Create a Python 3.10+ venv first: `python3.11 -m venv .venv && source .venv/bin/activate && pip install "asyncssh>=2.23.0"`
  </how-to-verify>
  <resume-signal>Type "approved" to confirm asyncssh is legitimate, or "reject" with reason</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add SSH config fields to Settings class (D-06)</name>
  <files>src/ascend_agent/config.py</files>
  <read_first>
src/ascend_agent/config.py    — current Settings class (22 lines), exact structure and field order

.planning/phases/04-reproduction-capability/04-PATTERNS.md   — Section 5 lines 282-324 for exact code pattern, import change needed

.planning/phases/04-reproduction-capability/04-CONTEXT.md   — lines 32-34 for D-06 SSH config spec
  </read_first>
  <action>
Per D-06, add four new fields to the Settings class in `src/ascend_agent/config.py`:

1. Update the import at line 4: change `from pydantic_settings import BaseSettings, SettingsConfigDict` to also import `Field` from pydantic:
   `from pydantic_settings import BaseSettings, SettingsConfigDict`
   Add a new line: `from pydantic import Field`

2. Insert the following four fields AFTER the `mcp_server_command` field (line 14) and BEFORE the `model_post_init` method (line 16):

   Field 1: `ssh_host: str = Field(default="", description="SSH hostname for remote execution")`
   Field 2: `ssh_user: str = Field(default="", description="SSH username for remote execution")`
   Field 3: `ssh_key_path: str = Field(default="", description="Path to SSH private key file (fallback if agent unavailable)")`
   Field 4: `shell_timeout: int = Field(default=60, ge=1, description="Default timeout in seconds for shell commands")`

These fields are automatically mapped to env vars ASCEND_SSH_HOST, ASCEND_SSH_USER, ASCEND_SSH_KEY_PATH, ASCEND_SHELL_TIMEOUT via the existing `env_prefix="ASCEND_"` on line 8.

Do NOT modify existing fields (python_version, platform, env_vars, repo_path, mcp_server_command). Do NOT modify `model_post_init`.
  </action>
  <verify>
    <automated>python3 -c "from ascend_agent.config import Settings; s = Settings(); assert hasattr(s, 'ssh_host') and hasattr(s, 'ssh_user') and hasattr(s, 'ssh_key_path') and hasattr(s, 'shell_timeout') and s.shell_timeout == 60"</automated>
  </verify>
  <acceptance_criteria>
- `src/ascend_agent/config.py` contains `ssh_host: str = Field(default=""` on a line between `mcp_server_command` and `model_post_init`
- `src/ascend_agent/config.py` contains `ssh_user: str = Field(default=""` 
- `src/ascend_agent/config.py` contains `ssh_key_path: str = Field(default=""`
- `src/ascend_agent/config.py` contains `shell_timeout: int = Field(default=60, ge=1`
- `from pydantic import Field` is present at module level (line 4 area)
- Running `python3 -c "import os; os.environ['ASCEND_SSH_HOST']='test'; from ascend_agent.config import Settings; s=Settings(); assert s.ssh_host=='test'"` succeeds
- `Settings().shell_timeout` returns 60 (default)
- `Settings().ssh_host` returns "" (default)
- Existing fields (python_version, platform, env_vars, repo_path, mcp_server_command) are unchanged
- `model_post_init` method is unchanged
  </acceptance_criteria>
  <done>Settings class has ssh_host, ssh_user, ssh_key_path, shell_timeout fields mapped to ASCEND_ prefixed env vars</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → Settings | Untrusted environment variable input crosses into application config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | config.py SSH key path | mitigate | SSH key path stored in env var ASCEND_SSH_KEY_PATH, never hardcoded; asyncssh reads key file directly — key material never logged or echoed |
| T-04-06 | Tampering | config.py env vars | mitigate | Pydantic Field validation: shell_timeout uses `ge=1` to prevent zero/negative; empty string defaults for SSH fields accepted as "not configured" with explicit checks before use |
| T-04-SC | Tampering | pip install asyncssh | mitigate | Human checkpoint (Task 2) verifies package legitimacy on PyPI before install; [ASSUMED] tag from research due to slopcheck unavailability |

**T-04-05 disposition: mitigate.** SSH key path comes from ASCEND_SSH_KEY_PATH env var only. The Settings class stores it as a string attribute; asyncssh reads the key file directly via client_keys parameter. No key material is passed through command-line arguments, stdout, or logs.

**T-04-SC disposition: mitigate.** asyncssh is the only new package. Human checkpoint (Task 2) gates pip install behind PyPI verification: check package name, version, source repo, and stars.
</threat_model>

<verification>
Run the Settings import check: `python3 -c "from ascend_agent.config import Settings; s = Settings(); assert s.shell_timeout == 60; print('OK')"`
</verification>

<success_criteria>
1. pyproject.toml contains asyncssh>=2.23.0 in the dependencies list
2. asyncssh is pip-installed and importable in the project environment
3. Settings class exposes ssh_host, ssh_user, ssh_key_path, shell_timeout via ASCEND_ env prefix
4. shell_timeout defaults to 60 seconds with ge=1 validation
</success_criteria>

<output>
Create `.planning/phases/04-reproduction-capability/04-01-SUMMARY.md` when done
</output>
