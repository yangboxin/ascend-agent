---
phase: 07-chinese-model-integration
plan: 01
type: execute
wave: 1
depends_on: [06-01, 06-02]
files_modified:
  - src/ascend_agent/diagnosis/router.py
  - src/ascend_agent/config.py
  - tests/test_diagnosis/test_router.py
autonomous: true
requirements: [CHN-01, CHN-02, CHN-03]

must_haves:
  truths:
    - "create_router('deepseek') resolves ASCEND_DEEPSEEK_* env vars with default base_url https://api.deepseek.com/v1"
    - "create_router('qwen') resolves ASCEND_QWEN_* env vars with default base_url https://dashscope.aliyuncs.com/compatible-mode/v1"
    - "DeepSeek default model is deepseek-v4-flash (configurable via ASCEND_DEEPSEEK_DEFAULT_MODEL)"
    - "Qwen default model is qwen-turbo (configurable via ASCEND_QWEN_DEFAULT_MODEL)"
    - "Non-openai providers raise ValueError with provider name when API key is missing"
    - "Settings class has deepseek_api_key, deepseek_base_url, deepseek_default_model, qwen_api_key, qwen_base_url, qwen_default_model"
  artifacts:
    - path: "src/ascend_agent/diagnosis/router.py"
      provides: "PROVIDER_DEFAULTS dict + updated create_router()"
      contains: "PROVIDER_DEFAULTS.*deepseek"
    - path: "tests/test_diagnosis/test_router.py"
      provides: "create_router tests for deepseek and qwen"
      min_lines: 150

---

<objective>
Add provider-specific default base URLs and default models for DeepSeek and Qwen to `create_router()`, add corresponding Settings fields, and test all provider creation paths.

**Purpose:** Enable `--provider deepseek` and `--provider qwen` to work out of the box without requiring explicit `ASCEND_DEEPSEEK_BASE_URL` or `ASCEND_QWEN_BASE_URL` env vars. Users only need to set the API key.

**Output:** Updated `router.py` with PROVIDER_DEFAULTS mapping, updated `config.py` with provider fields, passing tests for both providers.
</objective>

<execution_context>
@/Users/ybx/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ybx/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-chinese-model-integration/07-CONTEXT.md
@.planning/phases/06-provider-routing-foundation/06-CONTEXT.md
@.planning/phases/06-provider-routing-foundation/06-PATTERNS.md
@src/ascend_agent/diagnosis/router.py
@src/ascend_agent/config.py
@tests/test_diagnosis/test_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROVIDER_DEFAULTS dict and update create_router() with provider-specific base URLs and default models</name>

  <files>
    src/ascend_agent/diagnosis/router.py
  </files>

  <read_first>
    @src/ascend_agent/diagnosis/router.py
    @.planning/phases/07-chinese-model-integration/07-CONTEXT.md (D-01 through D-12)
  </read_first>

  <action>
    Modify `src/ascend_agent/diagnosis/router.py`:

    1. Add a PROVIDER_DEFAULTS dict after the ProviderConfig class (before create_router function):
    ```python
    PROVIDER_DEFAULTS: dict[str, dict[str, str]] = {
        "openai": {"base_url": "https://api.openai.com/v1", "default_model": "gpt-4o"},
        "deepseek": {"base_url": "https://api.deepseek.com/v1", "default_model": "deepseek-v4-flash"},
        "qwen": {"base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1", "default_model": "qwen-turbo"},
    }
    ```

    2. Update `create_router()` to use PROVIDER_DEFAULTS instead of hardcoded OpenAI defaults:
    - Replace `base_url=base_url or "https://api.openai.com/v1"` with `base_url=base_url or PROVIDER_DEFAULTS.get(provider, {}).get("base_url", "https://api.openai.com/v1")`
    - Replace `default_model=os.environ.get(f"{prefix}_DEFAULT_MODEL", "gpt-4o")` with `default_model=os.environ.get(f"{prefix}_DEFAULT_MODEL", PROVIDER_DEFAULTS.get(provider, {}).get("default_model", "gpt-4o"))`

    3. No other changes to create_router logic. The openai key fallback (PROV-04) and non-openai key validation stay unchanged.

    Verify with test command before declaring task done.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_diagnosis/test_router.py -v --no-header -q 2>&1 | tail -15</automated>
  </verify>

  <done>
    - PROVIDER_DEFAULTS dict exists in router.py with openai, deepseek, qwen entries
    - create_router("deepseek") default base_url is "https://api.deepseek.com/v1"
    - create_router("deepseek") default model is "deepseek-v4-flash"
    - create_router("qwen") default base_url is "https://dashscope.aliyuncs.com/compatible-mode/v1"
    - create_router("qwen") default model is "qwen-turbo"
    - All existing create_router tests still pass
  </done>

  <acceptance_criteria>
    - `grep -c "PROVIDER_DEFAULTS" src/ascend_agent/diagnosis/router.py` returns >= 1
    - `grep "deepseek" src/ascend_agent/diagnosis/router.py | grep -c "api.deepseek.com"` returns 1
    - `grep "qwen" src/ascend_agent/diagnosis/router.py | grep -c "dashscope.aliyuncs.com"` returns 1
    - `grep "deepseek-v4-flash" src/ascend_agent/diagnosis/router.py` returns 1
    - `grep "qwen-turbo" src/ascend_agent/diagnosis/router.py` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add DeepSeek and Qwen Settings fields to config.py</name>

  <files>
    src/ascend_agent/config.py
  </files>

  <read_first>
    @src/ascend_agent/config.py
    @.planning/phases/07-chinese-model-integration/07-CONTEXT.md (D-11)
  </read_first>

  <action>
    Modify `src/ascend_agent/config.py`:

    Add these fields after the existing `openai_base_url` field (under a `# Phase 7: Chinese provider config fields` comment):

    ```python
    # Phase 7: Chinese provider config fields
    deepseek_api_key: str = Field(
        default="",
        description="DeepSeek API key (env: ASCEND_DEEPSEEK_API_KEY)",
    )
    deepseek_base_url: str = Field(
        default="",
        description="DeepSeek base URL override (env: ASCEND_DEEPSEEK_BASE_URL)",
    )
    deepseek_default_model: str = Field(
        default="",
        description="DeepSeek default model override (env: ASCEND_DEEPSEEK_DEFAULT_MODEL)",
    )
    qwen_api_key: str = Field(
        default="",
        description="Qwen DashScope API key (env: ASCEND_QWEN_API_KEY)",
    )
    qwen_base_url: str = Field(
        default="",
        description="Qwen DashScope base URL override (env: ASCEND_QWEN_BASE_URL)",
    )
    qwen_default_model: str = Field(
        default="",
        description="Qwen default model override (env: ASCEND_QWEN_DEFAULT_MODEL)",
    )
    ```

    Verify that `python3 -c "from ascend_agent.config import settings; print('OK')"` works.
  </action>

  <verify>
    <automated>python3 -c "from ascend_agent.config import settings; print('Settings OK:', hasattr(settings, 'deepseek_api_key'))"</automated>
  </verify>

  <done>
    - Settings has deepseek_api_key, deepseek_base_url, deepseek_default_model fields
    - Settings has qwen_api_key, qwen_base_url, qwen_default_model fields
    - Import works without error
  </done>

  <acceptance_criteria>
    - `grep "deepseek_api_key" src/ascend_agent/config.py` returns 1
    - `grep "qwen_api_key" src/ascend_agent/config.py` returns 1
    - `grep "deepseek_default_model" src/ascend_agent/config.py` returns 1
    - `grep "qwen_default_model" src/ascend_agent/config.py` returns 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add create_router tests for DeepSeek and Qwen providers</name>

  <files>
    tests/test_diagnosis/test_router.py
  </files>

  <read_first>
    @tests/test_diagnosis/test_router.py
    @.planning/phases/07-chinese-model-integration/07-CONTEXT.md (D-01 through D-12)
  </read_first>

  <action>
    Add these new test functions to `tests/test_diagnosis/test_router.py` (at end of file):

    1. `test_create_router_deepseek_defaults(monkeypatch)`:
    - Set `ASCEND_DEEPSEEK_API_KEY` to "sk-deepseek"
    - Mock `openai.OpenAI.__init__` to capture kwargs
    - Call `create_router("deepseek")`
    - Assert `captured["base_url"] == "https://api.deepseek.com/v1"`
    - Assert `captured["api_key"] == "sk-deepseek"`
    - Check router._model is "deepseek-v4-flash"

    2. `test_create_router_deepseek_custom_base_url(monkeypatch)`:
    - Set `ASCEND_DEEPSEEK_API_KEY` to "sk-deepseek"
    - Set `ASCEND_DEEPSEEK_BASE_URL` to "https://custom.deepseek.com/v1"
    - Mock `openai.OpenAI.__init__` to capture kwargs
    - Call `create_router("deepseek")`
    - Assert captured base_url is custom URL

    3. `test_create_router_deepseek_custom_model(monkeypatch)`:
    - Set `ASCEND_DEEPSEEK_API_KEY` to "sk-deepseek"
    - Set `ASCEND_DEEPSEEK_DEFAULT_MODEL` to "deepseek-v4-pro"
    - Mock `openai.OpenAI.__init__` to capture kwargs
    - Call `create_router("deepseek")`
    - Assert router._model is "deepseek-v4-pro"

    4. `test_create_router_qwen_defaults(monkeypatch)`:
    - Set `ASCEND_QWEN_API_KEY` to "sk-qwen"
    - Mock `openai.OpenAI.__init__` to capture kwargs
    - Call `create_router("qwen")`
    - Assert `captured["base_url"] == "https://dashscope.aliyuncs.com/compatible-mode/v1"`
    - Assert router._model is "qwen-turbo"

    5. `test_create_router_deepseek_missing_key(monkeypatch)`:
    - Delete `ASCEND_DEEPSEEK_API_KEY`
    - Call `create_router("deepseek")`
    - Assert raises ValueError with "ASCEND_DEEPSEEK_API_KEY"

    6. `test_create_router_qwen_missing_key(monkeypatch)`:
    - Delete `ASCEND_QWEN_API_KEY`
    - Call `create_router("qwen")`
    - Assert raises ValueError with "ASCEND_QWEN_API_KEY"

    Follow existing test patterns:
    - Use `monkeypatch.setenv` / `monkeypatch.delenv`
    - Mock `openai.OpenAI.__init__` with `lambda self, **kwargs: None`
    - Use `from ascend_agent.diagnosis.router import create_router`

    Verify with test command before declaring task done.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_diagnosis/test_router.py -v --no-header -q 2>&1 | tail -20</automated>
  </verify>

  <done>
    - 6 new test functions for DeepSeek and Qwen provider creation
    - Test default base URLs, custom URLs, custom models, missing keys
    - All existing tests still pass
  </done>

  <acceptance_criteria>
    - `grep -c "test_create_router_deepseek" tests/test_diagnosis/test_router.py` returns >= 3
    - `grep -c "test_create_router_qwen" tests/test_diagnosis/test_router.py` returns >= 1
    - `grep -c "deepseek" tests/test_diagnosis/test_router.py` returns >= 3
    - `grep -c "qwen" tests/test_diagnosis/test_router.py` returns >= 2
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → create_router | Provider-specific env vars (ASCEND_DEEPSEEK_API_KEY) determine which API key the router uses |
| ProviderConfig → OpenAI client | ProviderConfig fields are passed directly to OpenAI client constructor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | error messages | mitigate | Missing key errors include provider name but never include actual key values. Messages are templated strings. |
| T-07-02 | Tampering | PROVIDER_DEFAULTS | accept | Dict is a hardcoded constant — cannot be modified at runtime. Only code changes alter defaults. |
| T-07-03 | Tampering | no new dependencies | mitigate | Plan adds zero new pip packages. All changes use existing dependencies (openai SDK, pydantic, pydantic-settings). |
</threat_model>

<verification>
Run all router tests: `python3 -m pytest tests/test_diagnosis/test_router.py -v --no-header -q 2>&1`
Verify deepseek base URL default: `python3 -c "from ascend_agent.diagnosis.router import PROVIDER_DEFAULTS; print(PROVIDER_DEFAULTS['deepseek']['base_url'])"`
Verify qwen base URL default: same check for qwen
</verification>

<success_criteria>
- PROVIDER_DEFAULTS dict in router.py with openai, deepseek, qwen entries
- create_router("deepseek") uses https://api.deepseek.com/v1 by default and deepseek-v4-flash model
- create_router("qwen") uses https://dashscope.aliyuncs.com/compatible-mode/v1 by default and qwen-turbo model
- Settings has all 6 new provider fields (deepseek + qwen × 3 each)
- 6 new tests cover both providers with defaults, custom URLs, custom models, and missing keys
- All tests pass (existing + new)
</success_criteria>
