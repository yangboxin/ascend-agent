---
phase: 05-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ascend_agent/diagnosis/models.py
  - src/ascend_agent/config.py
  - tests/test_verification/__init__.py
  - tests/test_verification/conftest.py
  - tests/test_verification/test_models.py
autonomous: false
requirements:
  - VERIF-01
  - VERIF-02
user_setup:
  - service: pytest-json-report
    why: "Structured JSON test output parsing (D-03)"
    env_vars: []
    dashboard_config:
      - task: "Verify package legitimacy"
        location: "https://pypi.org/project/pytest-json-report/1.5.0/"
      - task: "Copy install command"
        location: "Run `pip install pytest-json-report>=1.5.0`"

must_haves:
  truths:
    - "VerificationResult model validates with status, hypothesis_id_verified, counts, and test details"
    - "TestDetail model validates with nodeid, outcome, duration, and message"
    - "Settings class has test_timeout field defaulting to 300"
    - "pytest-json-report is installed and importable"
  artifacts:
    - path: "src/ascend_agent/diagnosis/models.py"
      provides: "VerificationResult + TestDetail Pydantic models"
      contains: "class VerificationResult"
    - path: "src/ascend_agent/config.py"
      provides: "test_timeout Settings field"
      contains: "test_timeout"
    - path: "tests/test_verification/__init__.py"
      provides: "Test package marker"
      min_lines: 0
    - path: "tests/test_verification/conftest.py"
      provides: "Shared test fixtures (mock_router, mock_settings, sample_reproduction, sample_repo_dir)"
    - path: "tests/test_verification/test_models.py"
      provides: "VerificationResult and TestDetail model validation tests"
      exports: ["test_verification_result_valid", "test_verification_result_invalid_status"]
    - path: "tests/test_verification/"
      provides: "Test package directory"
  key_links:
    - from: "src/ascend_agent/diagnosis/models.py"
      to: "VerificationResult.status pattern"
      via: "Field(pattern=r\"^(pass|fail|error|timeout|no_tests)$\")"
    - from: "src/ascend_agent/config.py"
      to: "Settings class"
      via: "Field(default=300, ge=1)"
    - from: "tests/test_verification/conftest.py"
      to: "ReproductionResult fixture"
      via: "sample_reproduction with files_changed"
---

<objective>
Establish the data contracts and test infrastructure for Phase 5 verification.

**Purpose:** VerificationResult is the output contract consumed by the verify CLI (05-03) and the run_test MCP tool (05-03). TestDetail is the per-test detail sub-model. The test_timeout config is needed by VerificationEngine (05-02). The test package skeleton enables parallel test development.

**Output:** VerificationResult + TestDetail models in diagnosis/models.py, test_timeout in config.py, pytest-json-report installed, and tests/test_verification/ package with conftest fixtures and model tests (6+ tests green).
</objective>

<execution_context>
@/Users/ybx/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/ybx/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verification/05-CONTEXT.md
@.planning/phases/05-verification/05-RESEARCH.md
@.planning/phases/05-verification/05-PATTERNS.md
@src/ascend_agent/diagnosis/models.py
@src/ascend_agent/config.py
@tests/test_reproduction/conftest.py
@pyproject.toml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Install pytest-json-report (Package Legitimacy Gate)</name>
  <files>pyproject.toml</files>
  <read_first>
src/ascend_agent/diagnosis/models.py — current model patterns for context
.planning/phases/05-verification/05-RESEARCH.md — Package Legitimacy Audit section (lines 87–99), Standard Stack table (lines 56–68)
pyproject.toml — current dependency list for insertion location
  </read_first>
  <action>
Install pytest-json-report v1.5.0 per D-03. This is the ONLY new Phase 5 dependency — all others (pytest, Pydantic, Rich, Typer, FastMCP) are already installed.

PRESENT to user for package legitimacy verification (package is tagged [ASSUMED] — slopcheck was unavailable at research time):

```
Package: pytest-json-report v1.5.0
Registry: PyPI (https://pypi.org/project/pytest-json-report/1.5.0/)
Source repo: github.com/numirias/pytest-json-report
First released: 2018 (8+ years old)
Downloads: Established
Purpose: Structured JSON test output for pytest (required by D-03)
Rationale: Mature, well-maintained, recommended as successor to unmaintained pytest-json
Risk: LOW — 8-year track record, active maintenance, used by Phase 5 VerificationEngine to parse test results
```

After user approves:
1. Run `pip install "pytest-json-report>=1.5.0"`
2. Verify installation: `python -c "import pytest_jsonreport; print(pytest_jsonreport.__version__)"` — must output version string
3. Add `"pytest-json-report>=1.5.0"` to pyproject.toml `[project]` dependencies or `[project.optional-dependencies]` dev section
</action>
  <what-built>pytest-json-report v1.5.0 installed and verified</what-built>
  <how-to-verify>
1. Run `python -c "import pytest_jsonreport; print(pytest_jsonreport.__version__)"` — should print "1.5.0" (or compatible version)
2. Run `pip show pytest-json-report` — confirms package metadata
3. Check pyproject.toml has the dependency entry
  </how-to-verify>
  <acceptance_criteria>
- `python -c "import pytest_jsonreport"` exits 0 with no ImportError
- `pip show pytest-json-report` shows version ≥ 1.5.0
- pyproject.toml contains `pytest-json-report>=1.5.0` in dependencies section
  </acceptance_criteria>
  <resume-signal>Type "approved" after verifying the package at pypi.org/project/pytest-json-report/1.5.0/ — or type concerns</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add VerificationResult + TestDetail models and test_timeout config</name>
  <files>
src/ascend_agent/diagnosis/models.py
src/ascend_agent/config.py
  </files>
  <read_first>
src/ascend_agent/diagnosis/models.py — existing Pydantic model patterns (ReproductionResult at lines 144–164, Evidence at lines 6–14, Hypothesis at lines 17–24), ConfigDict(extra="forbid") convention, Field usage
src/ascend_agent/config.py — existing Settings class structure, Field pattern for shell_timeout at line 19
.planning/phases/05-verification/05-PATTERNS.md — VerificationResult model structure (lines 268–314), TestDetail model (lines 268–278), config pattern (lines 430–440)
.planning/phases/05-verification/05-RESEARCH.md — VerificationResult model fields (lines 383–425), D-03 requirement
  </read_first>
  <behavior>
    - Test 1: VerificationResult with all required fields validates (status="pass", hypothesis_id_verified=0, command="pytest", summary="passed", duration_seconds=1.0)
    - Test 2: VerificationResult rejects invalid status outside pattern (status="invalid")
    - Test 3: VerificationResult forbids extra fields (raises ValidationError)
    - Test 4: VerificationResult defaults: tests_found=0, tests_run=0, passed=0, failed=0, errors=0, exit_code=-1, files_tested=[], stdout=""
    - Test 5: VerificationResult rejects negative duration_seconds (raises ValidationError)
    - Test 6: TestDetail validates with nodeid="tests/test_core.py::test_add", outcome="passed", duration=0.01, message=None
    - Test 7: TestDetail forbids extra fields (raises ValidationError)
    - Test 8: Settings().test_timeout defaults to 300
  </behavior>
  <action>
Add VerificationResult + TestDetail models to src/ascend_agent/diagnosis/models.py:

1. Insert TestDetail class BEFORE VerificationResult (TestDetail is referenced by VerificationResult.tests field)
2. Insert both models AFTER ReproductionResult (line 164) and BEFORE DiagnosisOutput (line 167)
3. TestDetail fields: nodeid: str (required), outcome: str (required), duration: float | None (default=None), message: str | None (default=None)
4. VerificationResult fields per RESEARCH.md lines 383–425:
   - status: str with pattern=r"^(pass|fail|error|timeout|no_tests)$"
   - hypothesis_id_verified: int
   - framework: str | None (default=None)
   - command: str
   - summary: str
   - tests_found: int (default=0)
   - tests_run: int (default=0)
   - passed: int (default=0)
   - failed: int (default=0)
   - errors: int (default=0)
   - skipped: int (default=0)
   - xfailed: int (default=0)
   - xpassed: int (default=0)
   - tests: list[TestDetail] (default_factory=list)
   - exit_code: int (default=-1)
   - duration_seconds: float (ge=0.0)
   - files_tested: list[str] (default_factory=list)
   - stdout: str (default="")
5. Both models use model_config = ConfigDict(extra="forbid") — match existing project convention
6. All fields use Field(description=...) with explicit descriptions
7. Use from __future__ import annotations at top of file (already present, line 1)

Add test_timeout to src/ascend_agent/config.py:
1. Insert AFTER shell_timeout on line 19: test_timeout: int = Field(default=300, ge=1, description="Default timeout in seconds for test execution")
2. Follow the exact same Field pattern as shell_timeout — no extra flags

Update mock_settings fixture in conftest.py: add mock.test_timeout = 30 to match the shell_timeout pattern
</action>
  <verify>
    <automated>pytest tests/test_verification/test_models.py -x -v</automated>
  </verify>
  <acceptance_criteria>
- `python -c "from ascend_agent.diagnosis.models import VerificationResult, TestDetail"` exits 0
- `python -c "from ascend_agent.config import Settings; s = Settings(); assert s.test_timeout == 300"` exits 0
- VerificationResult.model_validate({"status":"pass","hypothesis_id_verified":0,"command":"pytest","summary":"ok","duration_seconds":0.5}) succeeds
- VerificationResult.model_validate({"status":"no_tests","hypothesis_id_verified":0,"command":"","summary":"no tests","duration_seconds":0.0}) succeeds (tests default values)
- VerificationResult.model_validate_json('{"status":"invalid"}') raises ValidationError (status pattern check)
- TestDetail.model_validate({"nodeid":"t::test","outcome":"passed"}) succeeds
- All model tests pass: python -m pytest tests/test_verification/test_models.py -x exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create test verification package skeleton with conftest and model tests</name>
  <files>
tests/test_verification/__init__.py
tests/test_verification/conftest.py
tests/test_verification/test_models.py
  </files>
  <read_first>
tests/test_reproduction/conftest.py — fixture patterns (mock_router, sample_diagnosis, sample_repo_dir, mock_settings)
tests/test_reproduction/test_models.py — model test patterns (valid/invalid/forbid-extra/defaults/duration-negative tests)
.planning/phases/05-verification/05-PATTERNS.md — conftest pattern (lines 444–536), test_models patterns (lines 618–756)
src/ascend_agent/diagnosis/models.py — VerificationResult and TestDetail models (just added in Task 2)
  </read_first>
  <action>
Create test_verification package directory: tests/test_verification/

Create tests/test_verification/__init__.py: empty file (0 bytes) — matches project convention (all test packages use empty init)

Create tests/test_verification/conftest.py with four fixtures following test_reproduction/conftest.py patterns:

1. mock_router fixture: Mock() with mock.completion = Mock() (verbatim from test_reproduction/conftest.py lines 13-17)

2. mock_settings fixture: Mock with shell_timeout=10, test_timeout=30, ssh_host="", ssh_user="", ssh_key_path="" (extend reproduction pattern, add test_timeout)

3. sample_reproduction fixture: ReproductionResult with status="success", command="python -m pytest tests/test_shell_exec.py", stdout="1 passed", stderr="", exit_code=0, duration_seconds=1.5, hypothesis_id_tested=0, files_changed=["src/ascend_agent/tools/shell_exec.py"]

4. sample_repo_dir fixture (tmp_path): create a temp dir with:
   - pyproject.toml containing [tool.pytest.ini_options] testpaths=['tests'], asyncio_mode='auto'
   - src/mypkg/core.py: def add(a, b): return a + b
   - tests/test_core.py: from src.mypkg.core import add; def test_add(): assert add(2,3)==5

Create tests/test_verification/test_models.py with 7 test functions following test_reproduction/test_models.py patterns:

1. test_verification_result_valid: construct full VerificationResult with TestDetail, assert status="pass", passed=1
2. test_verification_result_invalid_status: status="invalid_status" → pytest.raises(ValidationError)
3. test_verification_result_forbids_extra: add extra_field → pytest.raises(ValidationError)
4. test_verification_result_defaults: status="no_tests", hypothesis_id_verified=0, command="", summary="No tests", duration_seconds=0.0 → assert tests_found==0, tests_run==0, passed==0, failed==0, errors==0, exit_code==-1, files_tested==[], stdout==""
5. test_verification_result_duration_negative_rejected: duration_seconds=-0.1 → pytest.raises(ValidationError)
6. test_test_detail_valid: TestDetail(nodeid="tests/test_core.py::test_add", outcome="passed", duration=0.01, message=None) → assert all fields
7. test_test_detail_forbids_extra: add extra_field → pytest.raises(ValidationError)

Import models inside test functions (not top-level) for late-binding to avoid import-time validation issues — follow existing test pattern from test_reproduction/test_models.py.
</action>
  <verify>
    <automated>pytest tests/test_verification/test_models.py -x -v</automated>
  </verify>
  <acceptance_criteria>
- tests/test_verification/__init__.py exists (0 bytes)
- tests/test_verification/conftest.py exports fixtures: mock_router, mock_settings, sample_reproduction, sample_repo_dir
- tests/test_verification/test_models.py contains 7 test functions
- All 7 model tests pass: pytest tests/test_verification/test_models.py -x exits 0
- Each fixture can be imported: python -c "from tests.test_verification.conftest import mock_router, mock_settings, sample_reproduction, sample_repo_dir" (may need pytest runtime) — OR verify via: pytest tests/test_verification/ --collect-only returns 7+ tests
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip→venv | External PyPI package (pytest-json-report) installed into project environment |
| client→model | Untrusted JSON input deserialized into ReproductionResult/VerificationResult |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-05-01 | Tampering | diagnosis/models.py | mitigate | Pydantic v2 ConfigDict(extra="forbid") on VerificationResult and TestDetail — rejects unknown fields; Field(pattern=...) on status enforces allowed values; Field(ge=0.0) on duration_seconds blocks negative; Field(ge=1) on test_timeout blocks zero/negative timeouts | MEDIUM |
| T-05-02 | Spoofing | diagnosis/models.py | mitigate | ReproductionResult.model_validate_json() validates all fields before ingestion — malformed or forged JSON is rejected at parse boundary | LOW |
| T-05-SC | Tampering | pip install pytest-json-report | mitigate | Package legitimacy checkpoint (Task 1) — human must verify package at pypi.org/project/pytest-json-report/1.5.0/ before install; package has 8-year track record on PyPI | MEDIUM |
</threat_model>

<verification>
Wave 1 gate: `pytest tests/test_verification/test_models.py -x -v` exits 0 (7 tests green)
</verification>

<success_criteria>
- [x] VerificationResult Pydantic model added to diagnosis/models.py with 17 fields and regex-validated status
- [x] TestDetail Pydantic model added with 4 fields and extra="forbid"
- [x] test_timeout: int = Field(default=300, ge=1) added to Settings in config.py
- [x] pytest-json-report v1.5.0 installed and importable
- [x] tests/test_verification/ package created: __init__.py, conftest.py (4 fixtures), test_models.py (7 tests)
- [x] All 7 model tests pass
</success_criteria>

<output>
Create `.planning/phases/05-verification/05-01-SUMMARY.md` when done
</output>
