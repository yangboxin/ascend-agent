---
phase: 02-diagnosis-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/ascend_agent/cli/diagnose.py
  - tests/test_cli.py
autonomous: false
requirements:
  - DIAG-01
  - DIAG-02
user_setup:
  - service: openai
    why: LLM-driven diagnosis engine needs API key
    env_vars:
      - name: OPENAI_API_KEY
        source: OpenAI Dashboard -> API Keys
      - name: ASCEND_DIAGNOSIS_MODEL
        source: Optional. Defaults to gpt-4o. Set to gpt-4o-mini for cheaper diagnoses.
must_haves:
  truths:
    - CLI `ascend-agent diagnose run` calls Engine.diagnose() and displays DiagnosisResult
    - Rich-formatted output shows top 3 hypotheses with evidence (file:line + code snippets)
    - Partial failures are displayed, not silently swallowed
    - CLI integration test passes without real LLM calls
    - Engine runs silently - no prompts or clarifying questions in CLI output
  artifacts:
    - path: "src/ascend_agent/cli/diagnose.py"
      provides: Modified diagnose_run to invoke Engine and display results
      contains: "Engine("
      contains: "DiagnosisResult"
    - path: "tests/test_cli.py"
      provides: Integration test for CLI diagnosis command
      contains: "def test_cli_diagnose_integration"
  key_links:
    - from: "src/ascend_agent/cli/diagnose.py"
      to: "src/ascend_agent/diagnosis/engine.py"
      via: "import Engine"
      pattern: "from ascend_agent.diagnosis.engine import Engine"
    - from: "src/ascend_agent/cli/diagnose.py"
      to: "src/ascend_agent/diagnosis/router.py"
      via: "import ModelRouter"
      pattern: "from ascend_agent.diagnosis.router import ModelRouter"
    - from: "tests/test_cli.py"
      to: "src/ascend_agent/cli/diagnose.py"
      via: "pytest test imports app"
      pattern: "from ascend_agent.cli.app import app"
---

<objective>
Wire the Engine into the existing `diagnose run` CLI command and add Rich-formatted display for diagnosis results. After building the ContextDocument (Phase 1), the CLI now invokes Engine.diagnose() and displays the ranked hypotheses with evidence snippets. Includes a human-verify checkpoint for end-to-end visual verification.

Purpose: Complete the DIAG-01/DIAG-02 pipeline - user provides trace, CLI builds context, Engine diagnoses, Rich displays results. This is the final integration that makes the diagnosis capability usable.
Output: Modified `src/ascend_agent/cli/diagnose.py` with Engine wiring and Rich display; integration test in `tests/test_cli.py`.
</objective>

<execution_context>
@/Users/yangboxin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/yangboxin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-diagnosis-engine/02-CONTEXT.md
@.planning/phases/02-diagnosis-engine/02-RESEARCH.md
@.planning/phases/02-diagnosis-engine/02-VALIDATION.md

<interfaces>
Key types consumed by the CLI (created in Plans 02-01 and 02-02):

From src/ascend_agent/diagnosis/engine.py:
- Engine(router: ModelRouter, repo_path: str)
- Engine.diagnose(context_doc: ContextDocument) -> DiagnosisResult

From src/ascend_agent/diagnosis/models.py:
- DiagnosisResult: hypotheses (list[Hypothesis]), errors (list[PartialFailure]), iterations_used (int)
- Hypothesis: root_cause (str), evidence (list[Evidence]), confidence (float)
- Evidence: file_path (str), line_number (int), code_snippet (str), relevance (str)
- PartialFailure: stage (str), reason (str), details (str | None)

From src/ascend_agent/diagnosis/router.py:
- ModelRouter(model: str | None = None, api_key: str | None = None)
- ModelRouter.completion(messages, response_model, ...) -> BaseModel

Existing CLI code structure (src/ascend_agent/cli/diagnose.py):
- diagnose_run() typer command - _one_shot_mode() builds ContextDocument, displays it
- _repl_mode() - interactive REPL
- _display_context() - Rich table for repo info, trace frames
- console = Console() - Rich console instance
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: Wire Engine into CLI and add Rich result display</name>
<files>
src/ascend_agent/cli/diagnose.py
</files>
<read_first>
src/ascend_agent/cli/diagnose.py - full file: existing structure, imports, _one_shot_mode, _display_context, Rich patterns
src/ascend_agent/diagnosis/engine.py - Engine class, diagnose() signature
src/ascend_agent/diagnosis/models.py - DiagnosisResult, Hypothesis, Evidence, PartialFailure structure
src/ascend_agent/diagnosis/router.py - ModelRouter constructor
src/ascend_agent/config.py - settings singleton for env vars
</read_first>
<action>
Modify `src/ascend_agent/cli/diagnose.py`:

### 1. Add imports at module top:
Import these three at module level (alongside existing imports):
- `Engine` from `ascend_agent.diagnosis.engine`
- `ModelRouter` from `ascend_agent.diagnosis.router`  
- `Hypothesis`, `Evidence` from `ascend_agent.diagnosis.models`

Also add `from rich.syntax import Syntax` to the existing Rich imports if not already present.

### 2. Modify `_one_shot_mode()` function:
After the existing `_display_context(doc)` call and before the `if output_path is not None:` block, insert:

- Print a cyan status message: `"Running diagnosis..."` in bold
- Wrap in try/except ValueError:
  - try: instantiate ModelRouter() with no args, then Engine(router=router, repo_path=repo), then engine.diagnose(doc). Call `_display_diagnosis(result)`.
  - except ValueError as e: print error in red plus a yellow hint message about setting OPENAI_API_KEY env var. Then raise typer.Exit(code=1).

### 3. Add `_display_diagnosis(result: DiagnosisResult)` function:
Use Rich components (Panel, Syntax, Table, console.print) matching existing code style:

- Print a bold header: "Diagnosis Results" and "Search iterations used: {N}/3" in cyan.
- If result.errors is non-empty: render a red Panel titled "Partial Failures" listing each PartialFailure with stage, reason, and optional details.
- For each hypothesis in result.hypotheses (top 3):
  - Render a Panel with title "Hypothesis #{N} - Confidence: {confidence:.0%}"
  - Set border_style based on confidence: green if >=0.7, yellow if >=0.4, red otherwise.
  - Content: bold "Root Cause" label followed by hypothesis.root_cause
  - For each evidence: blue "File: {file_path}:{line_number}", then Syntax(code_snippet, "python", theme="monokai", line_numbers=True), then italic relevance text.
- If hypotheses list is empty: display "No hypotheses could be generated." and explain using error messages (D-04: never empty without explanation).
- Footer: "Diagnosis complete." in dim text.

### 4. Import `rich.syntax.Syntax` — if not already imported in the file, add:
`from rich.syntax import Syntax`

### 5. Do NOT modify `_repl_mode()` or `_display_context()` or the output write block.
</action>
<acceptance_criteria>
- `python -c "from ascend_agent.cli.diagnose import _display_diagnosis; print('OK')"` exits 0
- grep for 'Engine(' in src/ascend_agent/cli/diagnose.py returns a line
- grep for 'ModelRouter(' in src/ascend_agent/cli/diagnose.py returns a line
- grep for '_display_diagnosis' in src/ascend_agent/cli/diagnose.py returns at least 2 lines (def + call)
- grep for 'from rich.syntax import Syntax' in src/ascend_agent/cli/diagnose.py returns a line
- grep for 'Syntax(' in src/ascend_agent/cli/diagnose.py returns a line
- No bare print() calls exist in the file (all output via console.print)
</acceptance_criteria>
<verify>
<automated>python -c "
import ast
with open('src/ascend_agent/cli/diagnose.py') as f:
    tree = ast.parse(f.read())
imports = set()
for node in ast.walk(tree):
    if isinstance(node, ast.ImportFrom):
        for alias in node.names:
            imports.add(alias.asname or alias.name)
assert 'Engine' in imports, 'Engine not imported'
assert 'ModelRouter' in imports, 'ModelRouter not imported'
funcs = {n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))}
assert '_display_diagnosis' in funcs, '_display_diagnosis not defined'
print('CLI code structure validated')
"</automated>
</verify>
<done>
CLI wiring complete: Engine called from _one_shot_mode, _display_diagnosis renders Rich-formatted results with Syntax highlighting
</done>
</task>

<task type="auto">
<name>Task 2: Add CLI integration test</name>
<files>
tests/test_cli.py
</files>
<read_first>
tests/test_cli.py - existing test patterns (using CliRunner, importing app from ascend_agent.cli.app)
src/ascend_agent/cli/diagnose.py - the modified version with Engine wiring
src/ascend_agent/diagnosis/engine.py - Engine class imports
</read_first>
<action>
Add a new test function to `tests/test_cli.py`:

`test_cli_diagnose_integration()` — This test verifies the CLI calls Engine.diagnose() using monkeypatching to avoid real LLM calls:

1. Import CliRunner from typer.testing
2. Import app from ascend_agent.cli.app
3. Use monkeypatch.setattr to replace the following before invocation:
   a. `ascend_agent.diagnosis.router.ModelRouter.__init__` - accept self but do nothing (no validation)
   b. `ascend_agent.cli.diagnose.Engine` - create a Mock that returns a mock DiagnosisResult

4. Create a mock DiagnosisResult to return:
   - hypotheses: list with one Hypothesis having:
     - root_cause: "Mock root cause"
     - evidence: list with one Evidence having file_path="main.py", line_number=10, code_snippet="def test():\\n    return 1", relevance="mock relevance"
     - confidence: 0.85
   - errors: empty list
   - iterations_used: 2

5. Invoke: `runner.invoke(app, ["diagnose", "run", str(tmp_path), "--trace-text", "ValueError: mock error"])`

6. Assertions:
   - result.exit_code == 0
   - "Diagnosis Results" in result.stdout
   - "Mock root cause" in result.stdout
   - "Confidence: 85%" in result.stdout
   - "iterations used: 2" in result.stdout (or similar)

Use `from unittest.mock import Mock, MagicMock` for mocking. Create the mock DiagnosisResult by constructing the actual model if possible, or use a MagicMock with spec.

Follow existing test pattern: function-scoped, inline imports, use tmp_path fixture.
</action>
<acceptance_criteria>
- `pytest tests/test_cli.py::test_cli_diagnose_integration -x -v` passes
- Test does not require OPENAI_API_KEY env var
- Test verifies Engine output appears in CLI stdout
</acceptance_criteria>
<verify>
<automated>pytest tests/test_cli.py::test_cli_diagnose_integration -x -v</automated>
</verify>
<done>
Integration test added to test_cli.py, verifies Engine output appears in CLI stdout, passes without API key
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 3: Human-verify full diagnosis pipeline output</name>
<what-built>
Complete Phase 2 pipeline: CLI -> ContextDocument -> Engine.diagnose() -> Rich diagnosis display
</what-built>
<how-to-verify>
1. Ensure OPENAI_API_KEY is set in your environment: `export OPENAI_API_KEY=sk-...`
2. Run the diagnosis command against a real Python repo with a stack trace:
   ```
   ascend-agent diagnose run ~/vllm-ascend --trace-text "Traceback (most recent call last):\n  File \"/home/user/vllm-ascend/vllm/entrypoints/openai/api_server.py\", line 245, in create_chat_completion\n    result = await engine.generate(prompt, sampling_params)\n  File \"/home/user/vllm-ascend/vllm/engine/async_llm_engine.py\", line 89, in generate\n    output = await self._run_engine(model_input)\n  File \"/home/user/vllm-ascend/vllm/engine/async_llm_engine.py\", line 156, in _run_engine\n    raise ValueError(\"Invalid dimension: expected 4096, got 8192\")\nValueError: Invalid dimension: expected 4096, got 8192"
   ```
3. Or use a trace file: `ascend-agent diagnose run ~/vllm-ascend --trace /path/to/trace.log`
4. Verify the output shows:
   - [ ] "Diagnosis Results" header with iteration count
   - [ ] Top 3 hypotheses ranked by confidence (each in a bordered panel)
   - [ ] Each hypothesis has Root Cause text
   - [ ] Each hypothesis has evidence with file:line references and code snippets (Syntax highlighted)
   - [ ] No error/crash if API key is missing (instead: clear error message about OPENAI_API_KEY)
5. If trace references files outside the repo, verify partial results display correctly (errors section shows, hypotheses still produced)
</how-to-verify>
<resume-signal>
Type "approved" to confirm the diagnosis output looks correct, or describe any issues to fix.
</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI (diagnose.py) -> Engine | User-provided trace text or file crosses into LLM processing |
| CLI stdout | Diagnosis results containing file paths and code snippets are displayed to terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05 | Information Disclosure | _display_diagnosis | mitigate | Only display file:line and code snippets from within repo_path. Evidence.file_path should be shown as relative path from repo where possible. Raw trace lines logged at DEBUG only, not INFO. |
| T-2-06 | Spoofing | CLI diagnose.run | mitigate | OpenAPI API key failure gives clear error message (ValueError caught). Missing key does not crash - shows actionable message. |
</threat_model>

<verification>
- `pytest tests/test_cli.py::test_cli_diagnose_integration -x -v` passes
- `python -c "from ascend_agent.cli.diagnose import _display_diagnosis; print('Import OK')"` exits 0
- `ascend-agent diagnose run --help` shows expected subcommand help
- Manual verify checkpoint confirms Rich output is formatted correctly
</verification>

<success_criteria>
1. `src/ascend_agent/cli/diagnose.py` imports Engine and ModelRouter, wires diagnosis into _one_shot_mode
2. `_display_diagnosis()` renders Rich-formatted output with hypothesis panels, Syntax-highlighted code, and error reporting
3. Missing OPENAI_API_KEY produces clear user-facing error (not crash)
4. `tests/test_cli.py` has passing integration test for diagnosis flow
5. Human-verify checkpoint confirms end-to-end pipeline works
</success_criteria>

<output>
Create `.planning/phases/02-diagnosis-engine/02-03-SUMMARY.md` when done
</output>
